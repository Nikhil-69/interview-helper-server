services:
  mongo:
    image: mongo:7
    container_name: interview-helper-mongo
    restart: unless-stopped
    ports:
      - "${MONGO_PORT:-27017}:27017"
    volumes:
      - interview-helper-mongo-data:/data/db

volumes:
  interview-helper-mongo-data:
